# KiCad
*-backups/
*.kicad_prl
*.kicad_pcb-bak
*.kicad_sch-bak
*.bak
*.bck
fp-info-cache
~*.*
_autosave-*
*.lck

# KiCad 10 autosave store — its own git repo, generated state, not source
.history/
_restore_backup_*/

# KiCad generated output (keep sources, not derivatives)
gerbers/

# Laser/fab output IS committed — see docs/fabrication.md. It is generated,
# but the point of this repo is that someone can make the board, and that
# needs the files the laser actually eats without installing KiCad first.
# Regenerate with the commands in docs/fabrication.md after any board change.
*.gbr
*.gbrjob
*.net

# Arduino / PlatformIO
build/
.pio/
.pioenvs/
.piolibdeps/
*.elf
*.hex
*.bin
*.map

# CAD
*.stl.bak
*~

# OS / editor
.DS_Store
Thumbs.db
.directory
*.swp
*.swo
.vscode/
.idea/
